# v2.1 CHANGE-SET — candidate amendments (2026-07-07) · NOT ADOPTED, NOT CANON

> **Top invariant (verbatim):** "The automation is responsible for improving the research state.
> The world is responsible for establishing knowledge."

**Status:** CANDIDATE AMENDMENTS awaiting **independent reconstruction by streams that did not
draft them** before any canonicalization — adoption-by-assertion would violate the change-set's
own rule. Nothing here is in force. The governance stopping rule stands: these route through the
normal process (core-touching items = Class-A) only after reconstruction.

## Amendments (as relayed; classification per required fix 1)
Core-touching (route through **Class-A**; steward's signature):
1. Two-coordinate status **+ separate write-authorities** (the confidence/warrant setter-split as
   enforced mechanism, not just labeling).
2. **Warrant-propagation rule** over the dependency graph.
3. **Assumption-provenance schema.**
4. **Self-improvement only on non-warrant axes.**
Ordinary (Class-B batch on reconstruction): remaining items of the change-set (body with the
drafting stream; attaches below on relay, append-only).

## Required fix 1 (point 7) — recorded as part of the candidate
Each amendment is classified core-vs-ordinary (above); the core-touching four go through Class-A;
and **the warrant-write-authority rule must live INSIDE the hash-guarded constitutional bundle** —
a rule protecting warrant that sits outside the protection is unprotected. (Same lesson as
boundary-list-inside-core: the definition of "protected" belongs inside the protection.)

## Required fix 2 (point 4) — recorded as part of the candidate
REPLACE: "intelligence improvements allowed when they stay confidence-side" (a judgment made by
the shipping party) WITH: **"the validator mechanically proves no code path can write warrant;
self-improvement is allowed only where that check passes."** Judgment-by-shipper becomes
proof-by-validator.

## Adoption path (none of it taken yet)
Independent reconstruction → core items to Class-A / ordinary items to Class-B batch → on
ratification, warrant-write-authority enters the manifest core → only then does any of this bind.
