# The statistics-contact: mechanism, existence, and the next closure   [non-canon · candidate · cross-model]

*What the plurality↔statistics contact MEANS: its mechanism, why physics answers as it does, how the shape relates
to actual existence, and — found in the framework's own terms — the shape of what is still missing. Warrant:
cross-model reasoned (blind Claude ∥ ChatGPT, ChatGPT live in steward Chrome; both converged). Mechanism / why /
shape = warranted-or-defensible; the next-gap = located hypothesis. **Null certificate INTACT** — the complex
Hilbert space + topology remain imports; nothing derived. Nothing forced/admitted; Tier-3. Provenance: 2026-07-07 chat.*

## Closure 1 — the mechanism is clear (cross-model: `mechanism-clear`, both)
A 3-hinge chain; the first hinge is the framework's, the rest are imports:
1. **(FRAMEWORK)** a unit `U=(C,O,Rep,P)` + the plurality move: form `n` identical copies, demand the joint
   invariant be well-defined under the relabeling symmetry ⇒ *the joint state must carry a definite representation
   of the exchange group.* This is the whole framework-internal content — a **question**.
2. **(IMPORT — complex Hilbert space / superposition)** states are rays in a **complex** Hilbert space, so
   "transforms consistently under relabeling" is realized projectively ⇒ exchange can only act by a **phase**.
3. **(IMPORT — configuration-space topology / spacetime dimension)** the available reps are fixed by
   `π₁(config space)`: `= Sₙ` in `d ≥ 3` (only trivial + sign ⇒ **bosons / fermions**), `= braid group Bₙ` in
   `d = 2` (continuum of phases, non-abelian ⇒ **anyons**).
4. **(IMPORT — locality / spin-statistics)** relativistic locality ties the choice to spin and forbids higher
   parastatistics ⇒ pins each **species**.

**Why physics answers as it does (both, same reasons):** a phase *because* rays in a complex Hilbert space;
exactly two in 3+1 d *because* the 1-dim unitary reps of `Sₙ` are trivial + sign; a continuum in 2d *because* a
double exchange is not the identity (the braid group replaces `Sₙ`); species *because* spin-statistics/locality.
**The framework supplies the question (hinge 1); physics supplies every hinge that turns "a definite rep" into
"phase, exactly-these, this-species" (hinges 2–4).**

## How the shape relates to actual existence (cross-model, defensible)
Both, independently: the shape is **FORM / CONSTRAINT — not cause, not identity.** Claude: it "selects among
possibilities without producing or exhausting the concrete existent." ChatGPT: it "identifies where a physical
mechanism must fit but does not by itself produce the physical phenomenon." The *same* formal constraint is
satisfied by physically inequivalent realizations (bosons / fermions / anyons), so it cannot be their cause or
their identity. This is **ontic structural realism**, held honestly: the shape is how any existence *with
plurality and indistinguishability* must be **organized**, not why it **is**.

## Closure 2 — the shape of what is missing (cross-model, located)
To upgrade contact → derivation, the framework must **supply, not import**, the structure that turns hinge 1 into
hinges 2–4. Both models named the missing piece the **same way, and both stressed it is NOT another invariant**:
- **ChatGPT:** "a functorial bridge from the abstract plurality object to physical state spaces that canonically
  constructs the exchange groupoid, its representation category, the relevant topology, and the compatible
  Hilbert-space/locality structure — a structure-preserving **semantic functor with coherence data**, not merely
  another invariant."
- **Claude:** the missing object is "the pair **(enriching base for `Rep` — over complex vector spaces,
  homotopical/topological structure on the space of copies)** that the current `U=(C,O,Rep,P)` leaves unspecified."

Same shape: **a forced *enrichment* + a forced *configuration-space geometry*** — i.e. the free data `(C, Rep)`
that the unit currently leaves open. In the framework's own recursion this is the recurring shape one level in:
**run the representation-invariance / plurality machinery on the *choice of enriching base itself*
(Set → ℂ-Vect → dagger-compact) and ask whether indistinguishability + coherence *force* the complex / dagger
(quantum) structure.**

**This next closure coincides with an existing hard program** — quantum reconstruction / categorical quantum
mechanics (Hardy 2001; Chiribella–D'Ariano–Perinotti informational axioms; Abramsky–Coecke dagger-compact
categories). That program is *not solved*: whether the quantum enrichment is *forced* or *chosen* is exactly the
open question. So the framework's next point of closure = **"is the ℂ-linear/dagger structure forced?"** — well-posed
in our terms, and live in the literature.

## Bottom line
Closure 1 is reached and cross-model confirmed: the contact is fully typed (**question-answered, mechanism-clear**),
and physics's "why" is reasoned and lives *entirely in the imports*. Closure 2 is *located*, not reached: the
missing piece has a definite shape — **the forced enrichment (quantum structure) + configuration-space geometry** —
which is exactly what would turn the imports into outputs. Until that enrichment is forced, the contact stays
**a question answered, not a physics derived.** Null certificate intact.

## Register / holds
Cross-model reasoned · non-canon · Closure-1 (mechanism/why) warranted; shape=form/constraint defensible
(structural realism); Closure-2 located hypothesis (coincides with quantum reconstruction / CQM) · **null
certificate INTACT — ℂ-Hilbert-space + topology remain imports, nothing derived** · nothing forced/admitted ·
Tier-3. Provenance: 2026-07-07 chat; RECURRING-SHAPE-PLURALITY-LADDER; FRAMEWORK-CAPSTONE; PLACEMENT-NOTE §6.
