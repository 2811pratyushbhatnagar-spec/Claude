# FRAMEWORK CONTEXT MAP — candidate registration (2026-07-07) · NOT CANON

> **Governing invariant (verbatim):** "minimizes reconstruction cost; does not reduce
> reconstruction requirements."

**Status pair:** confidence **High** (orientation index over ACCESSIBLE material — explicitly not
"comprehensive") · warrant **CANDIDATE** (assembled by the triangulation Claude stream,
ChatGPT-reviewed; NOT reconstructed by an independent stream; NOT adopted).

**Intended use:** reconstruction guide — a pointer to sources. **NOT a canonical reference, NOT
evidence, NOT citable as authority.** Any document citing this map as canon is in register error.

## Three-way gap taxonomy (preserved verbatim in structure)
- **UNAVAILABLE** — material the map cannot reach: the D-005..D-024 ledger, project-scoped chats,
  the private kernel repo, the live repo.
- **UNRECONSTRUCTED** — known only as summary, not transcript.
- **UNSETTLED** — genuinely open in the research itself: the selection principle; the
  plurality-of-nothing question.

## Body status
The full map body resides with the triangulation stream (chat-side); this registration records its
existence, invariant, register stamp, and taxonomy so the repo's record does not depend on
Dispatch paraphrase. On relay, the body attaches BELOW this header without altering it
(append-only); its warrant remains CANDIDATE until a stream that did not draft it reconstructs it.
