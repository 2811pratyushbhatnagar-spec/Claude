# Framework State Reconstruction — 2026-07-07   [non-canon · Class-D orientation artifact]

**Confidence:** High (over ACCESSIBLE material — the mounted *mirror*, not canon `Desktop/code/framework`).
**Warrant:** CANDIDATE — reconstructed by an independent refresh stream (Cowork) that did not draft the
07-07 candidates; NOT adopted, NOT evidence, NOT citable as authority. Any doc citing this as canon is in register error.
**Method:** primary artifacts read host-side via the Read tool (the sandbox mount serves stale/truncated reads); three read-only agents; provenance `FILE §section`. Chat-side ChatGPT material was **INACCESSIBLE** this run (Chrome extension disconnected).

---

## 1. STATE UPDATE (what the framework is now)

A full **operational governance layer (v2.0)** now sits over the research, and the mathematics has advanced to **Ledger A v0.7** with R1 audited and the enumeration carried to **n=4**. Nothing irreversible has been ratified: every Class-A act (R1 promotion, Ledger C admission, the governance freeze) remains steward-gated and open.

- **Governance (NEW):** Decision-Classes A/B/C/D (`DECISION-CLASSES.md`); a Stateless-Worker Contract (`WORKER-CONTRACT.md` v1.0) enforced by `Scripts/worker_guard.py`; a `questions.json` registry; `GOVERNANCE-VERSION.json` = **v2.0, `frozen: false`**; a Stage-0 deterministic gate that is now "THE router" (old 5-hourly automations superseded, per Ledger E 2026-07-06). Escalation is monotonic (D→C→B→A only); de-escalation needs a human `DECLASSIFICATIONS.md` row (currently none).
- **Mathematics (Ledger A v0.7, frozen):** R1 (strict F-eq ⇒ deterministic bijection pair, cardinality-free) — proof **VALID-WITH-GAPS**, 9 expositional gaps repaired into v0.7, **dual-agent accept (3 blinded Claude + 2 blinded ChatGPT)**; still **conjecture-grade pending Tier-3 promotion**. n=2/n=3/**n=4** enumerated across strict / partial-identity / free rungs.
- **Correspondence/Ontology:** Ledger C empty, **2 unadmitted candidates** (RET-∃ ↔ always-available-exit has a DRAFT map); Ledger D open-empty by design.
- **Process:** read-model/packaging (repo authoritative, UI/packages are deletable projections), event-time evidence packets, a staged free-rung-n4 reproduction package (handoff-ready, **not published**), reader-comprehension principle accepted / UI deferred.
- **Self-diagnosis:** `EVIDENCE-RIVER.md` flags **10 research vs 44 infrastructure events → "machinery is the primary activity."**

Provenance: `SNAPSHOT.md`; `GOVERNANCE-VERSION.json`; `DECISION-CLASSES.md`; `questions.json`; `LedgerA/ledger_A_canonical.md §Theorem R1`; `LedgerA/audits/R1-AUDIT-REPORT-2026-07-05.md §1–3`; `LedgerA/audits/FREE-RUNG-REPRODUCTION-2026-07-06.md`; `LedgerC/README.md`; `LedgerE/evidence_log.md`; `EVIDENCE-RIVER.md`.

## 2. DELTA REPORT (new/modified since the 2026-07-05 understanding)

**Governance — all NEW:**
- Decision-Classes A–D + `WORKER-CONTRACT.md` v1.0 + `worker_guard.py` enforcement (governed paths untouched; append-only files append-only; monotone classification). `MANIFEST.sha256.json`, `DEPENDENCIES.json`, `run_ledger.jsonl`.
- `GOVERNANCE-VERSION.json` v2.0 with `frozen:false`; **Q-GOV-V2-FREEZE** queued Class-A (a system may not self-execute its own constitutional freeze). Two milestones fire on the steward's signature: (1) "ratified"; (2) "enforcement ACTIVE" only after the guarded validator rejects an unauthorized core edit *post*-ratification.
- Stage-0 deterministic gate stood up as the router; "deterministic software decides whether AI is needed; a healthy idle repo spends ~no tokens."

**Mathematics — modified:**
- Ledger A **v0.2 → v0.7** (audit-driven patch; NO promotion taken). Nondet definition **pinned** to the any-effect reading (37, not 25).
- **R1 audit complete** — valid-with-gaps, theorem TRUE, **finiteness not needed**, 9 gaps repaired, zero numeric discrepancies incl. blind n=4.
- **n=4 enumerated:** strict **24 = 4!** and partial **209 = ΣC(4,k)²k!** are **externally-audited** (matched a *blind* prediction, per-k breakdown given); free-rung **992,696 / 984,279 nondet** is **cross-confirmed** but **Level-3 "not blind"** (expected output was disclosed — explicitly not "triangulated"). Single-source n=4 side-quantities await reproduction (F-sup 177,347; F-sub 423,054,463).
- `bf_null_certificate.md` (standing NULL): no Ledger-A survivor-count invariant is forced onto any physical quantity without an imported unit/interpretation; free-rung n=4 brute-force at ~0.5%.

**Process/candidates — NEW (07-06/07-07, nothing adopted):** read-model/packaging + evidence-river; staged free-rung-n4 package; reader-interface split (principle accepted, UI deferred); **2026-07-07** Framework Context Map + **v2.1 change-set** (two-coordinate status, warrant-propagation over the dependency graph, assumption-provenance) — **candidates logged, NOTHING ADOPTED**, awaiting reconstruction by a non-drafting stream.

**Absorbed from the 07-05 automation work:** `.automation/TRACES.md`, the file-trace rule, and `compute_check.py` are now referenced inside `WORKER-CONTRACT.md` / the append-only set.

## 3. CONTRADICTIONS & CORRECTIONS

1. **CORRECTION (mine, earlier this session):** I flagged `status.json` as "A = 0.1/0.2, possible drift." **Withdrawn.** `status.json` reads **A = 0.7** and is consistent; the low number was the **mirror lagging canon** (seeded at v0.2 via `808393a`, re-mirrored on 07-05). Ground truth = host file tools / canon, never the sandbox `bash` mount. (`LedgerE/evidence_log.md` 2026-07-05 rows; `status.json`.)
2. **Mirror ≠ canon (standing caveat):** canon is `Desktop/code/framework`; the mounted folder is a mirror that syncs *behind* it. This reconstruction is from the mirror.
3. **Front-queue lag (minor):** `priorities.md` and `.automation/class_b_queue.md` (dated 07-05) do not list the 07-07 candidates — consistent with "not adopted," but the visible queues trail by two days.
4. **Banding vintage-lag (not a numeric conflict):** `counts.json` (updated 07-04, pre-audit) bands some n=3 runs "builder-only" while the audit reports them "cross-confirmed." Numbers agree everywhere.
5. **R1 audit says "Ledger A remains frozen at v0.2":** true *at authorship* (07-05); its prescribed patch produced v0.3→v0.7 the same window. Not a live contradiction.

## 4. NEXT HIGHEST-VALUE TASK (from the research state, not assumed intent)

The system's own evidence-river says **machinery, not research, is the primary activity** — so the highest-value move is research evidence that needs **no steward decision** and raises auditability:

> **Independent, BLIND reproduction of the n=4 free-rung count (992,696 total / 984,279 nondet)** by a from-scratch enumerator distinct from Impl A/B, with the expected value withheld from the implementer. Free-rung n=4 is currently only *cross-confirmed / Level-3 (not blind)*; a blind third implementation is the one move that can lift it toward **externally-audited** — a Class-C evidence act, fully within WORKER-CONTRACT, promoting nothing.

Everything else of high value is **steward-gated and should be surfaced, not done**: sign/decline **Q-GOV-V2-FREEZE**; promote/decline **R1** (audit + dual-agent accept are ready); admit/hold the **RET-∃ ↔ Exit** Ledger C map; relay the missing **`ledger_a_verify.py`** + **R3–R8** statements (the persistent Q-DIFF / Q-R3-R8 blockers).

## 5. EXPLICITLY INACCESSIBLE / UNRECONSTRUCTED (not inferred)

Canon repo `Desktop/code/framework` (only the mirror is mounted); the **D-005..D-024 decision ledger**, project-scoped chats, and the **private kernel / selection-boundary material** (declared UNAVAILABLE, `candidates/FRAMEWORK-CONTEXT-MAP-2026-07-07.md`); **`ledger_a_verify.py`** and **R3–R8** statements (never on disk); the **bodies** of the 07-07 Context Map and v2.1 change-set (only registration metadata on disk); **ChatGPT conversations** (Chrome disconnected this run). None of §1–4's load-bearing claims depend on these.

---
*Produced by the framework-refresh stream, 2026-07-07. Non-canon evidence artifact; regenerate to refresh. Promotion/admission/freeze remain Tier-3 — the steward's alone.*
