# Handoff — 2026-07-04 (repo stand-up)

**State:** Ledger A frozen v0.3 canonical (round-2 complete: R1 candidate general proof recorded +
banded conjecture-grade pending audit; partial-identity gradient 7/34; |S|=3 replication 6=3!;
determinism boundary = sub-diagonality of identity effects). Ledger B frozen v0.5. Ledger C empty
(1 unadmitted candidate). Ledger D open-empty. Ledger E converted to fixed schema, append-only.
Reconciliation 2026-07-04 frozen. Tier-1 checks green at commit time (`python run_checks.py`).

**Pending (owner actions):**
1. `ledger_a_verify.py` not on disk — exact-number diff of the R-series is pending the file; my counts
   (independent reproduction) are the diff targets, recorded in the frozen reconciliation snapshot.
2. Independent audit of the R1 general proof (five lines, `LedgerA/ledger_A_canonical.md` §Theorem R1).
3. R2 band-upgrade ratification (free halves = one-line general proofs; underivability halves =
   established-by-countermodel) — recorded, promotion HELD per firewall.
4. R3–R8 statements not on disk; reconciliation of those pending material.
5. Source folder (`Desktop\New folder (2)`) filename lags: `reversible_contact_protocol_ledger_v0.4.md`
   contains v0.5; `reversible_contact_v0.1.md` contains kernel v0.3. Repo copies are de-versioned
   (`LedgerB/protocol_ledger.md`, `LedgerB/kernel.md`); consider renaming sources or treating the repo
   as sole canonical location henceforth.

**Reproduce everything:** `python run_checks.py` from repo root.
